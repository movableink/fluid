import { Story } from "@storybook/addon-docs";

# Fluid Buttons


Fluid defines a set of CSS classes for styling buttons. These classes are shipped as part of our Tailwind CSS plugin and are meant to be used with Tailwind.

**Types**
- [default](#default-type)
- [primary](#primary)
- [destructive](#destructive)
- [outline](#outline)
- [plain](#plain)

**Sizes**
- [default](#default-size)
- [xs](#xs)
- [sm](#sm)
- [lg](#lg)
- [xl](#xl)

---

## Button Types

See each type to see specifics

### Default Type

This type is rendered when no `type:` param is passed with `.fluid-button` button.

<button className='fluid-button'>
  Basic
</button>

```hbs
<button class='fluid-button'>
  Basic
</button>
```

### Primary

This type is used for primary actions.

<button className='fluid-button type:primary'>
  Primary
</button>

```hbs
<button class='fluid-button type:primary'>
  Primary
</button>
```

### Destructive

This is used for destructive actions such as anything that would make the the action undoable like:
- Deleting an account
- Removing reports

<button className='fluid-button type:destructive'>
  Destructive
</button>

```hbs
<button class='fluid-button type:destructive'>
  Destructive
</button>
```

### Outline

The style to use when de-emphasizing the button.

The background is transparent, taking on the color of whatever is behind it.

<button className='fluid-button type:outline'>
  Outline
</button>

```hbs
<button class='fluid-button type:outline'>
  Outline
</button>
```

### Plain

The style to use when de-emphasizing the button.

The background is transparent, taking on the color of whatever is behind it.

<button className='fluid-button type:plain'>
  Plain
</button>

```hbs
<button class='fluid-button type:plain'>
  Plain
</button>
```

## Customization

The Plain button is can be easily customized to use a different text color, if need be. Any of the Tailwind text- utilities can be applied to override the original color.

<button className='fluid-button type:plain text-green-400 hover:text-green-500'>
  Click Me
</button>

```hbs
<button class='fluid-button type:plain text-green-400 hover:text-green-500'>
  Click Me
</button>
```

## Button Sizes

### Default Size

<button className='fluid-button'>
  Basic
</button>

```hbs
<button class='fluid-button'>
  Basic
</button>
```

### Extra Small

<button className='fluid-button size:xs'>
  Extra Small
</button>

```hbs
<button class='fluid-button size:xs'>
  Extra Small
</button>
```

### Small

<button className='fluid-button size:sm'>
  Small
</button>

```hbs
<button class='fluid-button size:sm'>
  Small
</button>
```

### Large

<button className='fluid-button size:lg'>
  Large
</button>

```hbs
<button class='fluid-button size:lg'>
  Large
</button>
```

### Extra Large

<button className='fluid-button size:xl'>
  Extra Large
</button>

```hbs
<button class='fluid-button size:xl'>
  Extra Large
</button>
```
